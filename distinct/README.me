# Distinct

Distinct is a task to create a function which accepts an array of integers and return the number of the distinct numbers in it, i.e. if the input
is [1,3,2,3,1] the result would be 3, as there are 3 distincts numbers in the array.

## Install

```
Install NodeJS (https://nodejs.org/en/)
```

## Run

```
node distinct.js (via cmd in the same dir as the file)
```